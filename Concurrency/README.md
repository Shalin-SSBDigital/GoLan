# Concurrency

Concurrency in Go — goroutines, channels, select, mutex, and the fan-out/fan-in pattern. Covers concurrency vs parallelism with Python comparisons.

**Files:**
- `concurrency.go` — Goroutines, channels, select, mutex, and fan-out/fan-in patterns