# Memory

Memory management in Go — stack vs heap allocation, escape analysis, and how Go's garbage collector works with Python comparisons.

**Files:**
- `memory.go` — Stack allocation, heap escape, slice internals, pointer receivers, memory map visualization