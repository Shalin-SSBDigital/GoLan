# Go Interface Internals — iface & eface

Go's internal interface representation — eface (empty interface/any) and iface (method interface with itab). Covers memory layout, type assertions, and the "nil is not nil" bug.

**Files:**
- `LEARN.md` — Complete 14-section teaching guide on interface internals