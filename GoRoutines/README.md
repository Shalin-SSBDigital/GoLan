# GoRoutines

Goroutines in Go — lightweight concurrent functions, WaitGroup, closure capture gotchas, anonymous goroutines, and sequential vs concurrent timing comparisons.

**Files:**
- `goroutines.go` — Basic goroutines, WaitGroup, loop closure gotcha, anonymous goroutines, concurrent timing